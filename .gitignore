# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Variables de entorno (aunque hoy solo contiene valores VITE_* públicos,
# nunca debe versionarse — ver auditoría de seguridad 2026-07)
.env
.env.*

# Scripts operativos con credenciales o PII — NUNCA subir
apply-migration.ts
create-admin.ts
apply-rpc.ts
scratch/
cambiar_password.sql
query_usuarios.sql

# Documentos internos (mapa de vulnerabilidades, no publicar)
TinkuyHub-Auditoria-Tecnica.pdf

# Estado local de la CLI de Supabase
supabase/.temp/
